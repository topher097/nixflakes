{ ... }: {
  # This file exists for compatibility with home-manager imports
  # Home configurations are defined per-user in users/${username}/default.nix
  # and imported via the modules system in modules/
}
